/** Public session payload stored in a cookie (no secrets). */
export const AUTH_SESSION_COOKIE_NAME = "traffic-ui-session";

/** Tab-scoped password for `Authorization: username:password` on admin routes. */
export const AUTH_SECRET_STORAGE_KEY = "traffic-ui-auth-secret";

export const AUTH_SESSION_MAX_AGE_DAYS = 7;
